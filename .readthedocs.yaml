.readthedocs.yaml
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

sphinx:
  configuration-file: docs/source/conf.py

python:
  install:
    - methods:
        - editable
      path: .
      extra_requirements:
        - dev
